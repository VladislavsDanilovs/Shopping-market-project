import { ShoppingMarketValidators } from './shopping-market-validators';

describe('ShoppingMarketValidators', () => {
  it('should create an instance', () => {
    expect(new ShoppingMarketValidators()).toBeTruthy();
  });
});
